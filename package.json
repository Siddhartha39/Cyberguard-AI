{
  "name": "cyberguard-ai",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"const fs=require('fs'),cp=require('child_process');if(fs.existsSync('frontend')){cp.execSync('npm --prefix frontend run dev',{stdio:'inherit'})}else{cp.execSync('vite',{stdio:'inherit'})}\"",
    "build": "node -e \"const fs=require('fs'),cp=require('child_process');if(fs.existsSync('frontend')){cp.execSync('cd frontend && npm install && npm run build && cp -r dist ../dist 2>/dev/null || true',{stdio:'inherit'})}else{cp.execSync('npm install && npm run build',{stdio:'inherit'})}\"",
    "preview": "node -e \"const fs=require('fs'),cp=require('child_process');if(fs.existsSync('frontend')){cp.execSync('cd frontend && npm run preview',{stdio:'inherit'})}else{cp.execSync('vite preview',{stdio:'inherit'})}\""
  }
}
